# -------------------------------
# FBF Engine - CPU Base (Actions-safe)
# -------------------------------

# Core fetching / parsing
requests==2.32.5
python-dateutil==2.9.0.post0
pytz==2024.1

# Data + math (fast CPU wheels)
numpy==2.1.3
pandas==2.2.3
scipy==1.14.1

# ML (CPU)
scikit-learn==1.5.2
joblib==1.4.2

# Speedups for simulations
numba==0.60.0
tqdm==4.67.0

# Optional but useful for clean configs
pydantic==2.9.2
